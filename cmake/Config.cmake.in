@PACKAGE_INIT@

set(_@CMAKE_PROJECT_NAME@_supported_components "@SUPPORTED_COMPONENTS@")

foreach(_comp ${@CMAKE_PROJECT_NAME@_FIND_COMPONENTS})
  if(${_comp} STREQUAL "")
    continue()
  endif()
  if (NOT "${_comp}" IN_LIST _@CMAKE_PROJECT_NAME@_supported_components)
    set(${_comp}_FOUND False)
    set(${_comp}_NOT_FOUND_MESSAGE "Unsupported component: ${_comp}")
  endif()
  include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@${_comp}Targets.cmake")
endforeach()
